services:
  - type: web
    name: webrtc-signaling
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn signaling:app --host 0.0.0.0 --port 10000