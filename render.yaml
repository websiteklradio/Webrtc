services:
  - type: web
    name: webrtc-signaling
    runtime: python
    plan: free
    buildCommand: pip install -r signaling/requirements.txt
    startCommand: uvicorn signaling.signaling:app --host 0.0.0.0 --port $PORT
    autoDeploy: true